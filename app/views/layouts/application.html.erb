  <!DOCTYPE html>
<html>
  <head>
    <title>Project1</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <nav>

      <% if @current_user.present? %>
        
        |<div class='log'>
        <%= link_to 'Logout', login_path, method:'delete', class: 'log' %> |
        <%= link_to "Users", users_path, class:'log'%> |
        <%= link_to "All Currently Borrowed items", loans_path%>|
        <% else %>
        <%= link_to 'Login', login_path, class: 'log' %> ||
        <%= link_to 'Sign Up', new_user_path, class: 'log' %> |
        <% end %>
        </div>
        <div id='title'>
        <h1>Game Library</h1>
        <% if @current_user.present? %>
          Welcome,<%=link_to @current_user.name, root_path, class:'name' %>
          <%end %>
      </div>
        <div class='lib'>
      <%= link_to "Games", games_path, class: 'lib'%>||

      <%= link_to "Categories", categories_path, class: 'lib'%>
      <!-- <div class='search'> -->
            <%= form_tag(search_page_path, :method => "get",
            class: 'navbar-form navbar-left')%>
            <!-- <div class="input-group"> -->

              <%= search_field_tag :search, params[:search], placeholder: "Search for Categories", class: "form-control" %>
            <!-- <div class="input-group-btn"> -->
              <%= button_tag "Search!", :class => 'btn btn-info glyphicon glyphicon-search',:name => nil%>
            </div>
          <!-- </div> -->

      <!-- </div> -->




      <hr>




    </nav>
    <div class='page'>
    <%= yield %>
    </div>
  </body>
</html>
